import {Fragment} from "react";

export default function Index() {
    return (
        <section className="text-center text-red-500 font-bold w-full">
            این صفحه فعلا در دسترس نیست
        </section>
    );
};
